// Shared Tailwind preset. Real semantic tokens land in Step 3 via @gdggye/design-tokens.
// Until then this preset is intentionally empty so apps can already extend it.
/** @type {import('tailwindcss').Config} */
module.exports = {
  content: [],
  theme: {
    extend: {},
  },
  plugins: [],
};
